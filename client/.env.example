# Backend API URL — full URL including /api path
# In development, the Vite dev proxy handles /api, so this can be left unset.
# In production (Vercel), set this to your Render backend URL:
#   VITE_API_URL=https://your-app.onrender.com/api
VITE_API_URL=
